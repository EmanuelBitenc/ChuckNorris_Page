import { Box, Text } from "@chakra-ui/react";
import { useEffect, useState } from "react";

export default function ApiData({ selectedCategory, value, url }) {
  return <Box color="white"></Box>;
}
